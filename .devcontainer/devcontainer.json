{
  "name": "ADA Grav Theme Development",
  "dockerFile": "Dockerfile",
  "forwardPorts": [8000],
  "mounts": ["source=${localWorkspaceFolder},target=/workspace,type=bind"],
  "remoteUser": "www-data",
  "workspaceFolder": "/workspace",
  "postStartCommand": "cd /var/www/html && php -S 0.0.0.0:8000 system/router.php",
  "customizations": {
    "vscode": {
      "extensions": ["mblode.twig-language", "esbenp.prettier-vscode"],
      "settings": {
        "editor.defaultFormatter": "esbenp.prettier-vscode",
        "editor.formatOnSave": true,
        "[html]": {
          "editor.defaultFormatter": "mblode.twig-language"
        }
      }
    }
  }
}
