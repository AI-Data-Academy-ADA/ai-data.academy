{% do assets.addJs('theme://js/navbar.js', { loading: 'defer'}) ~ compress %}

<div class="navbar-brand">
	<a class="navbar-item" href="/">
		<img class="dark-brand" src="{{ url('theme://images/logo_dark.svg') }}" alt="Ada Project Logo"/>
		<img class="light-brand" src="{{ url('theme://images/logo.svg') }}" alt="Ada Project Logo"/>
	</a>

	<a role="button" class="navbar-burger is-light" aria-label="Navigationsmenü" aria-expanded="false" data-target="navbarMain">
		<span aria-hidden="true"></span>
		<span aria-hidden="true"></span>
		<span aria-hidden="true"></span>
		<span aria-hidden="true"></span>
	</a>
</div>

<div id="navbarMain" class="navbar-menu">
	<div class="navbar-start">
		{% for p in pages.children.visible %}
			{% set is_current = (p.active or p.activeChild) ? 'is-light' : 'is-text' %}
			<div class="navbar-item">
				<a class="button is-rounded {{ is_current }}" href="{{ p.url }}">{{ p.menu|e }}</a>
			</div>
		{% endfor %}
	</div>

	<div class="navbar-end">
		{% set is_day = page.route == "/ki-days" %}
		{% set is_festival = page.route == "/ki-festival" %}
		<div class="navbar-item">
			<a class="button is-rounded is-shadowless is-light uppercase-text pcb-button {{ is_day ? 'custom-active' : '' }}" href="/ki-days">
				KI-Days
			</a>
			<a class="button is-rounded is-primary uppercase-text pcb-button {{ is_festival ? 'custom-active' : '' }}" href="/ki-festival">
				KI-Festival
			</a>
		</div>
		<div class="navbar-item has-text-right">
			<button class="button is-text is-rounded" onclick="toggleTheme()" id="themeToggle">🌙 / ☀️</button>
		</div>
	</div>
</div>
