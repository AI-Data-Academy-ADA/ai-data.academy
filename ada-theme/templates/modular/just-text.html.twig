{% do assets.addCss('theme://css/modular.just-text.css') ~ compress %}

<section class="{{ page.header.indent_text == true ? 'indent-text' : '' }} content section">
	{{ page.content|raw }}
</section>
